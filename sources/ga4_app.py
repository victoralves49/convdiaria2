"""GA4 App property — extração de visitantes únicos e novos usuários.

A ser implementado na fase 2:
    def fetch_users(start_date: date, end_date: date) -> dict[date, dict[str, int]]:
        '''Retorna {date: {"totalUsers": ..., "firstOpens": ...}}.'''
"""
